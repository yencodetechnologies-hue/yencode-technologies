PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret

# Login credentials (as requested). In production, hash the password
# and store users in a real database instead of .env.
ADMIN_EMAIL=yencodetechnologies@gmail.com
ADMIN_PASSWORD=123456
